import { useState } from "react";

export const Card = () => {
  return (
    <div
      style={{
        width: "10em",
        height: "5em",
        border: "2px solid pink",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      lul
    </div>
  );
};

export default Card;
